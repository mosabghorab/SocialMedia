// enum for user auth status >>>
enum AuthStatus { AUTH_COMPLETED, AUTH_NOT_COMPLETED }

// enum for user auth type >>>
enum SignInMethod { EMAIL_AND_PASSWORD, GOOGLE, FACEBOOK }

// enum for user status >>>
enum UserStatus { ONLINE, OFFLINE, BUSY }

// enum for themes >>>
enum AppTheme { DARK, LIGHT }

// enum for type of image >>>
enum ImageType { PERSONAL_IMAGE, COVER_IMAGE }

// enum for image from where >>>
enum ImageFrom { CAMERA, GALLERY }

// enum for user gender >>>
enum UserGender { MALE, FEMALE }
